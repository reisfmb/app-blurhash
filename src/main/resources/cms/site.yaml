kind: "Site"
apis:
  - "asset"
  - "react4xp"
# Pages render through the page controller (cms/pages/default). Parts have no controllers of
# their own, so Content Studio's single-component requests are routed to cms/component.ts.
mappings:
  - controller: "/cms/component.js"
    service: "component"
    order: 10
